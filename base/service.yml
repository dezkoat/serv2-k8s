apiVersion: v1
kind: Service
metadata:
  name: nodejs-downstream-service
  argocd.argoproj.io/sync-options: Prune=false
spec:
  type: LoadBalancer
  selector:
    app: nodejs-downstream-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
